# set up

1. 打开 `:LazyExtras`
2. 把光标移动到这行：`○ lang.markdown`
3. 按 **`x`**
4. 看到它从 `○` 变成启用状态
5. 退出后让插件安装完成，然后重新打开 Markdown 文件

`lang.markdown` 这个 extra 里本来就包含了你要的两个东西：

* `markdown-preview.nvim`：浏览器预览 Markdown/公式
* `render-markdown.nvim`：在 Neovim 里做轻量渲染
